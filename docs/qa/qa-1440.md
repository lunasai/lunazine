# QA Audit — 1440px (large) breakpoint

Figma source: `portfolio_web` › frame `357:13081` (long scroll, 1440-wide sections)
Audit date: 2026-05-07 · **All items verified via MCP and resolved in codebase** (2026-05-07)

---

## 1. Global / Tokens — **RESOLVED**


| #   | What was wrong                     | Figma value (MCP)                                                                                                                                                                                    | Fix applied                                           |
| --- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| G-1 | Letter-spacing for display & title | `get_variable_defs` (374:3486): display/title text styles specify `letterSpacing: -1`; `get_design_context` (368:13137) resolves display text to `tracking-[-0.4px]` at 40 px (= −1 % of font size). | `--letter-spacing-tight: -0.01em` in `css/tokens.css` |


---

## 2. Navbar / Header — **RESOLVED**


| #   | What was wrong                             | Figma value (MCP)                                                                                                   | Fix applied                                                                                                  |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| N-1 | Nav links should use semantic navbar class | `get_design_context` (368:13137): Header nav items are `button` components with `h-[24px]`, mono 14 px, `py-[4px]`. | `<a class="navbar__link">` in `index.html`; `.navbar__link` matches height/padding in `component-navbar.css` |


---

## 3. Footer bar (`.bottom`) — **RESOLVED**


| #   | What was wrong          | Figma value (MCP)                                                                                 | Fix applied                                                                              |
| --- | ----------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| F-1 | Mode-toggle order       | `get_design_context` (368:13139): `light` control first (`374:8364`), `dark` second (`374:8234`). | Reordered buttons in `index.html`; `aria-pressed` unchanged per theme                    |
| F-2 | Inactive button opacity | Same node: inactive wrapper uses `opacity-80`.                                                    | `.bottom__mode .button[aria-pressed="false"] { opacity: 0.8 }` in `component-bottom.css` |


---

## 4. Intro section — **RESOLVED**


| #   | What was wrong       | Figma value (MCP)                                                                           | Fix applied                                                                |
| --- | -------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| I-1 | Accent phrase colour | `get_design_context` (368:13140): accent span `text-[#8eff8e]` (= `semantic/highlight/fg`). | `.intro__text .accent { color: var(--color-highlight-fg); }` in `main.css` |


---

## 5. Work sections — **RESOLVED**


| #   | What was wrong                            | Figma value (MCP)                                                                   | Fix applied                                                                           |
| --- | ----------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| W-1 | Metrics leading glyph                     | `get_design_context` (368:13207): metric rows use `=` (node `368:13208`).           | `=` + `.work__detail-arrow--metric` on metric rows in `index.html`                    |
| W-2 | Scroll indicator vertical bleed (panel 1) | `get_metadata` (368:13191): instance `368:13192` at `y="-125"`.                     | `#work .work__scroll { top: -125px; }` in `main.css`                                  |
| W-3 | Fourth panel scroll fill                  | `get_design_context` (374:8778): all three indicators solid accent.                 | `data-step="35"` on fourth `.scroll-section` in `index.html`                          |
| W-4 | Accent CTA control height                 | `get_design_context` (368:13280): button `h-[25px]` on email control (`368:13289`). | `.button--accent { height: 25px; box-sizing: border-box; }` in `component-button.css` |


---

## 6. About section — **RESOLVED**


| #         | What was wrong                | Figma value (MCP)                                                      | Fix applied                                                                                                 |
| --------- | ----------------------------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| A-1       | ASCII font                    | `get_design_context` (368:13280): `font-['DM_Mono:Light',sans-serif]`. | `--font-mono-ascii`, Google Fonts `DM Mono` wght 300, `font-family` + `font-weight: 300` on `.about__ascii` |
| A-2       | ASCII size                    | Same: `text-[length:var(--typography/footnote,12px)]`.                 | `--typography-footnote: 12px` in `tokens.css`; `.about__ascii` uses it                                      |
| A-3 / A-4 | Label / body / CTA spacing    | Same: `about me` container `gap-[12px]`.                               | `.about__content` flex column `gap: 12px`; removed extra label & CTA margins                                |
| A-5       | Bottom padding on about block | Same: `pb-[80px]` on `368:13286`.                                      | `padding-bottom: 80px` on `.about__content`                                                                 |


---

## Summary — **Cleared**

All tracked discrepancies for the 1440px reference frame were re-checked with Figma MCP (`get_variable_defs`, `get_design_context`, `get_metadata`) and addressed in HTML/CSS.