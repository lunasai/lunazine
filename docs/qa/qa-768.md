# QA Audit — Small (768px canvas)
Figma source: `portfolio_web` › responsive section **`357:11932`**
Audit date: 2026-05-07 · **Resolved**

---

## Summary

Verified tokens and scroll behaviour via MCP (`get_variable_defs` on **`357:12047`** intro; `get_design_context` on scroll instance **`357:11934`**).

| Check | Figma value (MCP) | Code |
|---|---|---|
| `spacing/margins` | **24** (`357:12047`) | `--spacing-margin: 24px` in `@media (max-width: 768px)` (`tokens.css`) |
| `typography/display` | **64** | `--typography-display: 64px` in same breakpoint |
| `typography/title` | **48** | `--typography-title: 48px` in same breakpoint |
| `typography/mono` | **14** | `--typography-mono: 14px` inherited from root |
| Intro hero text `y` | **216** (`357:12050`) | `.intro__content { padding-top: 216px }` (`main.css` ≤768) |
| Work stacked rails — scroll segments | Gradient on segment 1 only (matches Step **10**) | First panel keeps `data-step="10"` + existing `component-scroll-section.css` |

---

## Issues logged → **RESOLVED**

| ID | Section | What was wrong | Fix |
|---|---|---|---|
| S-1 | *(none new)* | MCP comparison showed parity with existing `@media (max-width: 768px)` rules for margins, type scale, intro offset, and stacked work layout selectors. | No code change required |
