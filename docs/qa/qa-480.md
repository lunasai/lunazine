# QA Audit — X-small (480px canvas)
Figma source: `portfolio_web` › responsive section **`346:5770`**
Audit date: 2026-05-07 · **Resolved**

---

## Summary

Verified narrowest mode with MCP (`get_variable_defs` on **`353:1132`** intro).

| Check | Figma value (MCP) | Code |
|---|---|---|
| `spacing/margins` | **16** | `--spacing-margin: 16px` in `@media (max-width: 480px)` (`tokens.css`) |
| `screen/max-width` token context | **480** | Matches `--screen-x-small: 480px` naming |
| `typography/display` | **48** | `--typography-display: 48px` in same breakpoint |
| `typography/title` | **32** | `--typography-title: 32px` in same breakpoint |
| `typography/mono` | **14** | `--typography-mono: 14px` inherited from root |
| Intro hero text `y` | **216** (`353:1173`) | Same `.intro__content { padding-top: 216px }` as 768 — matches Figma |

---

## Issues logged → **RESOLVED**

| ID | Section | What was wrong | Fix |
|---|---|---|---|
| XS-1 | *(none new)* | No additional `@media (max-width: 480px)` layout overrides are required in Figma for columns beyond what §768 already applies; display/title tighten via tokens. | Confirmed parity |
