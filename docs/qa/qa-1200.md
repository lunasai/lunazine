# QA Audit — Medium (1200px canvas)
Figma source: `portfolio_web` › responsive section **`357:11477`** (`screen/max-width` mode **1200**)
Audit date: 2026-05-07 · **Resolved**

---

## Summary

Cross-checked layout tokens and frames with Figma MCP (`get_variable_defs` on **`357:11592`** intro; `get_metadata` on **`357:11478`** first work panel).

| Check | Figma value (MCP) | Code |
|---|---|---|
| `spacing/margins` | **40** at intro node `357:11592` | `--spacing-margin: 40px` (default + `@media (max-width: 1200px)`) |
| `typography/display` | **32** | `--typography-display: 32px` inside `@media (max-width: 1200px)` in `tokens.css` |
| First work scroll widget `y` | **0** (`357:11479`) | `#work .work__scroll { top: -125px }` scoped to **`min-width: 1201px`** only (`main.css`) — no bleed at ≤1200 |
| Work panel height | **1024** (`357:11478`) | `@media (max-width: 1200px) { .section--work { height: 1024px } }` |
| Work content row offset | **≈373px** (`357:11491` `y`) | `.work__row { padding-top: 373px }` in same breakpoint |

---

## Issues logged → **RESOLVED**

| ID | Section | What was wrong | Fix |
|---|---|---|---|
| M-1 | Work — scroll rail | Long-scroll (−125px bleed) was applying at medium widths where Figma places scroll at **y = 0**. | Wrapped bleed rule in `@media (min-width: 1201px)` |

No further discrepancies found for this breakpoint after MCP verification.
